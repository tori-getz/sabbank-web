
import type { Routes } from '@typing';

import {
    RegisterScreen
} from '@screens';

export const registerRoutes: Routes = [
    {
        path: '/register',
        component: <RegisterScreen />
    }
]
